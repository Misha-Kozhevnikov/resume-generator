Ниже — готовый `README.md` для вашего Vite-проекта «Генератор резюме». Он содержит описание проекта, структуру, запуск, архитектуру, принципы работы модулей и заметки по расширению. Для Vite корректно описаны стандартные команды `dev`, `build` и `preview`, а для `localStorage` указано, что черновики очищаются через `Storage.clear()` / `Storage.removeItem()`. [vite](https://vite.dev/guide/build)

```md
# Генератор резюме

Веб-приложение на Vite и чистом JavaScript для пошагового создания резюме, выбора шаблона под профессию, получения подсказок по улучшению текста и экспорта результата в PDF.

## О проекте

**Цель проекта:**  
Создать удобную интерактивную систему, которая помогает пользователю быстро собрать профессиональное резюме по шагам, получить рекомендации по оформлению и сохранить результат в PDF.

**Целевая аудитория:**
- соискатели;
- студенты;
- специалисты, которые хотят обновить резюме;
- пользователи, которым нужен быстрый и понятный конструктор резюме.

## Возможности

- Пошаговое заполнение резюме.
- Шаблоны под разные профессии.
- Превью резюме в реальном времени.
- Рекомендации по заполнению и ATS-ключам.
- Сохранение черновика в `localStorage`.
- История сохранённых резюме.
- Экспорт в PDF.
- Модальное окно для уведомлений.

## Технологии

- **JavaScript** — основная логика приложения.
- **HTML/CSS** — структура и стили интерфейса.
- **Vite** — сборка и локальный запуск проекта.
- **jsPDF** — генерация PDF.
- **localStorage** — хранение черновика и истории в браузере.

## Структура проекта

```text
resume-generator-vite/
├─ index.html
├─ package.json
├─ vite.config.js
├─ README.md
├─ public/
│  └─ avatar-placeholder.png
└─ src/
   ├─ main.js
   ├─ App.js
   ├─ data/
   │  └─ templates.js
   ├─ lib/
   │  ├─ storage.js
   │  ├─ suggestions.js
   │  ├─ resumeBuilder.js
   │  └─ pdfExport.js
   └─ styles/
      └─ main.css
```

## Назначение файлов

### `index.html`
Точка входа для Vite.  
Содержит контейнер `#app`, в который монтируется приложение.

### `src/main.js`
Стартовый файл приложения:
- подключает стили;
- импортирует `App` и `initApp`;
- вставляет HTML в `#app`;
- запускает инициализацию обработчиков.

### `src/App.js`
Основной файл интерфейса и логики:
- формирует разметку приложения;
- управляет шагами формы;
- обрабатывает клики по кнопкам;
- обновляет превью;
- запускает модальное окно;
- сохраняет данные в `localStorage`.

### `src/data/templates.js`
Содержит список шаблонов резюме:
- IT-специалист;
- маркетолог;
- бухгалтер;
- инженер;
- менеджер.

### `src/lib/storage.js`
Модуль работы с `localStorage`:
- сохранение черновика;
- загрузка черновика;
- очистка черновика;
- сохранение истории;
- очистка истории.

### `src/lib/suggestions.js`
Генерация подсказок:
- анализ заполненности;
- рекомендации по тексту;
- ATS-ключевые слова.

### `src/lib/resumeBuilder.js`
Преобразование состояния формы в:
- объект резюме;
- HTML-превью;
- список навыков;
- экранированный текст для безопасного вывода.

### `src/lib/pdfExport.js`
Экспорт резюме в PDF:
- создаёт PDF с помощью `jsPDF`;
- формирует заголовок;
- выводит основные разделы;
- сохраняет файл на устройство пользователя.

### `src/styles/main.css`
Основные стили:
- адаптивная сетка;
- стили карточек;
- форма;
- кнопки;
- модальное окно;
- блок превью;
- индикатор прогресса.

### `public/avatar-placeholder.png`
Заглушка для фото профиля, если пользователь не указал собственное изображение.

## Установка

### 1. Установите зависимости
```bash
npm install
```

### 2. Запустите проект в режиме разработки
```bash
npm run dev
```

После запуска Vite покажет локальный адрес, обычно это:
- `http://localhost:5173`

### 3. Соберите проект для продакшена
```bash
npm run build
```

### 4. Проверьте сборку локально
```bash
npm run preview
```

## Как пользоваться

1. Откройте сайт в браузере.
2. Заполните личные данные.
3. Добавьте опыт работы.
4. Укажите образование.
5. Добавьте навыки, курсы, языки и описание.
6. Выберите шаблон под нужную профессию.
7. Посмотрите результат в превью.
8. Нажмите **Экспорт в PDF**.
9. При необходимости используйте **Сбросить черновик**.

## Архитектура приложения

Архитектура проекта модульная и построена по принципу разделения ответственности.

### 1. Слой представления
Отвечает за отображение интерфейса:
- `App.js` формирует HTML-разметку;
- `main.css` отвечает за внешний вид;
- превью резюме отображается отдельно от формы.

### 2. Слой состояния
Отвечает за данные формы:
- текущее состояние хранится в объекте `state`;
- повторяющиеся блоки опыта и образования хранятся в массивах;
- выбранный шаблон хранится отдельно через `templateSelect`.

### 3. Слой логики
Содержит:
- обработку кликов;
- пошаговую навигацию;
- добавление и удаление блоков;
- обновление превью;
- открытие и закрытие модалки;
- сохранение черновика.

### 4. Слой данных
Содержит статические настройки:
- список шаблонов;
- их названия;
- цвета;
- ключевые слова ATS;
- описания.

### 5. Слой хранения
Использует браузерное `localStorage`:
- черновик сохраняется между перезагрузками;
- история сохраняется отдельно;
- пользователь может полностью очистить данные.

## Как работает черновик

Черновик хранится в `localStorage` и обновляется при вводе данных.  
При повторном открытии страницы приложение загружает сохранённое состояние и восстанавливает форму.

Если нажать **Сбросить черновик**, приложение:
- очищает черновик;
- очищает историю;
- сбрасывает состояние формы;
- возвращает первый шаг;
- перерисовывает интерфейс.

## Как работает PDF-экспорт

Экспорт PDF выполняется через `jsPDF`:
- создаётся новый документ;
- в него пишутся данные резюме;
- добавляются разделы;
- файл сохраняется локально на устройство.

Если часть данных не заполнена, PDF всё равно создаётся, но с пустыми или сокращёнными блоками.

## Возможные проблемы и их решение

### Кнопки не работают
Проверьте:
- вызван ли `initApp()` в `src/main.js`;
- нет ли ошибок в консоли браузера;
- не перекрывает ли кнопки модалка или другой блок;
- стоит ли у кнопок `type="button"`.

### Черновик не сбрасывается
Проверьте:
- вызывается ли `storage.clearDraft()`;
- вызывается ли `storage.clearHistory()`;
- обновляется ли UI через `updateUI()`.

### Модальное окно не закрывается
Проверьте:
- работает ли обработчик на `#closeModalBtn`;
- не скрывает ли кнопку слой с `z-index`;
- применён ли класс `.hidden` с `display: none !important`.

## Команды npm

```bash
npm install
npm run dev
npm run build
npm run preview
```

## Требования к окружению

- Node.js актуальной версии, совместимой с Vite;
- npm;
- современный браузер с поддержкой ES-модулей и `localStorage`.

## Примечание

Данные сохраняются только в браузере пользователя и не отправляются на сервер.  
Это делает проект простым в запуске и удобным для локального использования.
